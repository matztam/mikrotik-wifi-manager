#ifndef CONFIG_H
#define CONFIG_H

// ==================== KONFIGURATION ====================

// WiFi Credentials (Ihr Bord-WLAN)
const char* WIFI_SSID = "Boot-WLAN";
const char* WIFI_PASSWORD = "IhrPasswort";

// MikroTik Credentials
const char* MIKROTIK_IP = "10.0.0.112";
const char* MIKROTIK_USER = "admin";           // Nur für Basic Auth (leer lassen bei Token)
const char* MIKROTIK_PASS = "";                // Nur für Basic Auth (leer lassen bei Token)
const char* MIKROTIK_TOKEN = "";               // REST API Token (empfohlen!) - siehe MIKROTIK-TOKEN-SETUP.md

// MikroTik WLAN Interface Name (z.B. "wlan1", "wlan2")
const char* MIKROTIK_WLAN_INTERFACE = "wlan1";

// Unterstützte WLAN-Bänder (abhängig von Hardware-Fähigkeiten)
// Für MikroTik Groove ohne AC: "2ghz-g/n" und "5ghz-a/n"
// Für neuere Hardware mit AC: "2ghz-g/n" und "5ghz-a/n/ac"
const char* BAND_2GHZ = "2ghz-g/n";      // Empfohlen: g/n (b ist veraltet)
const char* BAND_5GHZ = "5ghz-a/n";      // Oder "5ghz-a/n/ac" falls unterstützt

// ESP32 Webserver Port
const int WEB_PORT = 80;

#endif

; Local PlatformIO overrides for OTA uploads
; Copy this file to "platformio_secrets.ini" and adjust the values.
; Make sure "platformio_secrets.ini" is excluded from version control.

[env:esp32-s2-saola-1]
; IP address or mDNS hostname of the ESP32 for OTA uploads
upload_port = <ESP32_IP_OR_HOSTNAME>

; Additional flags passed to espota.py (set your OTA password here)
upload_flags =
    --auth=REPLACE_WITH_OTA_PASSWORD
